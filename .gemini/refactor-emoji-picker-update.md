Removed 'No Server Emojis' Notification

## Completed Actions

1.  **Code Cleanup in `emoji-picker.tsx`**:
    - Removed the conditional block that displayed "No server emojis found" with an upload link.
    - Removed the unused `ExternalLink` import.

## Result

The Emoji Picker no longer prompts users to upload emojis when none are found, simplifying the UI as requested. The codebase remains lint-free.
